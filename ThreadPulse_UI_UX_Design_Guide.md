# ThreadPulse: UI/UX 디자인 가이드 및 화면 구성서

첨부해주신 레퍼런스 이미지(다크 모드 기반의 프리미엄 웹사이트)의 세련된 무드와 ThreadPulse SaaS 플랫폼의 특성을 결합하여 기획한 UI/UX 구체화 문서입니다.

## 1. 디자인 시스템 (Design System)

### 1.1. 색상 팔레트 (Color Palette)
*   **Background (Main):** `#121212` 또는 `#0A0A0A` (깊이감 있는 솔리드 다크 톤)
*   **Background (Card/Surface):** `#1E1E1E` (글래스모피즘 틴트 또는 은은한 반투명 블러 적용)
*   **Text (Primary):** `#FFFFFF` (주요 헤딩 및 타이포그래피로 강렬한 대비 창출)
*   **Text (Secondary):** `#A0A0A0` (본문 및 부가 설명 텍스트)
*   **Accent Color:** 브랜드 신뢰감을 주는 모던한 블루 톤이나 Meta 스레드 특유의 분위기와 어울리는 그레이디언트 컬러를 포인트로 활용.

### 1.2. 타이포그래피 (Typography)
*   **Font Family:** `Pretendard`, `Inter`, `Outfit` 등 현대적이고 가독성이 뛰어난 산세리프 폰트 적용.
*   **Hero Heading:** 레퍼런스 이미지의 'INSIGHT, NERVE, VIEW'와 같이 과감하고 큼직한 타이포그래피(Big Typography)를 배치하여 프리미엄한 인상을 줌. 자간(Letter-spacing)을 넓게 하여 시각적인 여유 공간 확보.

---

## 2. 주요 화면 구성 요소 (Key Screen Compositions)

### 2.1. 랜딩 페이지 (Landing Page)
레퍼런스의 강렬한 첫인상과 카드 인터랙션을 반영한 마케팅 페이지 구성.

*   **Header (헤더):**
    *   좌측: ThreadPulse 로고 (미니멀하고 세련된 심볼).
    *   우측: 햄버거 메뉴(MENU) 아이콘 및 미니멀한 텍스트 네비게이션.
*   **Hero Section (히어로 섹션):**
    *   화면 중앙에 대형 텍스트로 핵심 가치 제안 배치 (예: "AUTOMATE YOUR THREADS, AMPLIFY YOUR PULSE").
    *   하단에 시선을 아래로 유도하는 중앙 스크롤 아이콘(마우스 모양의 마이크로 애니메이션) 배치.
*   **Feature Showcase (기능 소개 카드 섹션 - 이미지 참조):**
    *   레퍼런스 이미지 하단의 4분할 브랜드 카드와 유사하게, 전체 너비를 꽉 채우는 분할 그리드 레이아웃 적용.
    *   각 카드 영역에 '스마트 예약', 'AI 카피 생성', '크로스 부스팅', '다중 계정 관리' 등의 핵심 기능을 맵핑하여 백그라운드 이미지/영상을 은은하게 배치.
    *   **인터랙션:** 카드 영역에 마우스 호버 시 배경이 부드럽게 어두워지며 중앙에 'View More' 또는 관련 인터랙션 버튼이 나타남.

### 2.2. 대시보드 메인 (SaaS Dashboard)
실제 유저가 로그인 후 이용하는 다크 테마 기반의 관리 콘솔.

*   **GNB / LNB (네비게이션):**
    *   아이콘 위주의 슬림한 좌측 사이드바 구조 (Shadcn UI 스타일 적용, 필요시 축소/확장 가능).
    *   상단 탑바: 현재 선택된 워크스페이스, 알림(토큰 만료 등 알림), 프로필 설정 팝오버.
*   **Dashboard Summary (요약 현황판):**
    *   **Metrics:** 전체 계정의 노출수, 상호작용 지표를 보여주는 미니멀한 꺾은선 차트 (다크 테마 차트 라이브러리 활용).
    *   **Recent Campaigns:** 예약 대기 중이거나 발행 완료된 타래 리스트를 리스트 또는 카드 형태로 직관적으로 배치.

### 2.3. AI 스레드 빌더 창 (AI Thread Builder)
*   **Split View (분할 화면):**
    *   **좌측 (Control Panel):** 주제 키워드 입력, 어그로 레벨 조절 슬라이더, 페르소나 선택 드롭다운 폼 등 조작 영역.
    *   **우측 (Live Preview):** 좌측에서 생성된 글이 실제 스레드 모바일 앱 환경에서 어떻게 보이는지 보여주는 UI 프리뷰 모바일 목업 테두리 적용.

### 2.4. 스케줄링 캘린더 (Scheduling Calendar)
*   **UI 디자인:** 꽉 찬 격자의 캘린더 형태가 아닌, 다크 테마에 맞춰 테두리와 선을 최소화한 심리스(Seamless) 디자인 채택.
*   **인터랙션:** 생성된 캠페인을 드래그 앤 드롭으로 이동하여 날짜를 즉시 수정 가능.

---

## 3. UI/UX 인터랙션 및 애니메이션 (Micro-interactions)

*   **Hover Effect (호버 효과):** 주요 카드나 버튼에 마우스를 올릴 때 미세한 글로우(Glow) 효과나 배경색 블러 변경, 혹은 1.02배 스케일 업(Scale-up) 등 반응성 높은 애니메이션 부여.
*   **Scroll Animation:** 페이지 스크롤 시 텍스트 및 UI 요소들이 아래에서 위로 은은하게 나타나는 트랜지션(Fade-up)을 적용하여 동적이고 살아있는 느낌 제공 (Framer Motion 활용).
*   **No Placeholders:** 임시 텍스트나 빈 공간 대신, 실제 사용할 법한 더미 데이터와 화려한 톤의 에셋을 채워 넣어 UI 자체의 밀도감을 높임.
