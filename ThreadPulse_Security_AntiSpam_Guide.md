# ThreadPulse: Meta Threads API 보안 제재 회피 및 운영 가이드

본 문서는 자극적인 카피 및 자동화 발행 마케팅 솔루션의 특성상 발생할 수 있는 **Meta의 계정 영구 정지, API 제한 및 섀도우밴(Shadowban)** 리스크를 아키텍처 레벨에서 최소화하기 위한 필수 기술 가이드이다.

## 1. 메타 자동화 탐지 알고리즘의 주요 타겟
1. **정각/정분 발행 패턴:** 매일 정각 12시 00분, 15시 30분 등 정확한 시간에 정확한 인터벌로 글이 올라오면 봇(Bot)으로 감지되어 즉시 차단 우선순위에 오른다.
2. **동일 IP 내 복수 계정 스팸 활동:** 프록시나 분산 처리 없이 서버 단일 IP에서 1분 이내에 5~10개의 다른 계정이 번갈아가며 글을 쓰거나 특정 포스트에 다발적으로 댓글을 달면 서브넷 전체가 섀도우밴 대상이 된다.
3. **인간 불가능 속도의 상호작용 (초동 반응 매크로):** 원문이 게시된 지 1초 만에 3개의 계정이 동시다발적으로 인용 및 댓글을 다는 경우 기계적 매크로로 판정된다.

## 2. 시스템 아키텍처 필수 대응 스펙

### 2.1. 발송 타이밍 노이즈 주입 (Jittering Scheduler)
* **원리:** 유저가 대시보드 상에서 `오후 2시 10분`으로 발송 예약을 하더라도 데이터베이스 스케줄러는 이를 그대로 실행하지 않는다.
* **구현:** 백엔드 발송 워커는 아래 수식에 기반하여 가변 지연값을 연산하여 태스크를 지연 가동한다.
    $$\text{Actual\_Time} = \text{Target\_Time} + \text{Random}(-300\text{sec}, +300\text{sec})$$
* 초 단위까지 완벽히 쪼개진 인간 친화적 무작위 타임라인을 동적으로 매번 생성하여 메타 로그 상에 수동 업로드처럼 위장한다.

### 2.2. 교차 부스팅 순차 타임 가드 (Time-Safe Cross Engagement)
* 자가 교차 부스팅 기능을 작동시킬 때 서브 계정 간의 트래픽 간격을 최소 3분 이상으로 유지해야 한다.
* **동작 시나리오:**
    1. `00:00` - 메인 계정 원문 타래 최종 발행 완료.
    2. `00:04 (4분 후)` - 서브 계정 A가 원문을 인용(Quote)하며 논쟁적 이의 제기.
    3. `00:11 (7분 후)` - 서브 계정 B가 서브 계정 A의 인용글에 댓글로 반박.
    4. `00:19 (8분 후)` - 서브 계정 C가 메인 원문 아래에 중립적 요약 댓글 작성.
* 이러한 시나리오 기반 지연 큐 처리를 통해 자연스럽게 피드 참여자가 늘어나는 알고리즘 바이럴 유도를 성공시킨다.

### 2.3. Gemini API 기반 콘텐츠 텍스트 다변화 (Text Anti-Spam Checking)
* 동일하거나 유사한 카피라인이 반복되면 스팸 필터에 걸린다.
* Gemini API 연동 시 `temperature` 옵션을 최적의 창의성 구간인 `0.85`로 고정하고, 유저가 이전에 생성했던 카피 테스트 데이터와 대조하여 유사도가 80% 이상 겹치면 LLM에게 **재생성(Regenerate)** 요구 팩터를 자동으로 던지는 텍스트 필터 필드를 백엔드 미들웨어에 내장한다.
